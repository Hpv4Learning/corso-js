/**
 * ITERATORI
 * forEach, map, filter, reduce, find, some
 * Sono metodi che permettono di iterare gli array con finalità specifiche
 * Sono tra i metodi più importanti e maggiormente utilizzati nello sviluppo frontend
 * e fondamentali per approcciare i principali framework
 */

//Fino ad adesso abbiamo utilizzato questo per iterare array
const sport = [12, 56, 12, 6, 22, 44, 55, 66, 77, 88];

for (let i = 0; i < array.length; i++) {
  total += array[i];
  listaSpese.push(array[i]);
}
